name: gcf_projects_app
description: gcf Cost Management Application

version: 1.0.0+1

environment:
  sdk: ">=2.0.0-dev.68.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  dbcrypt: ^1.0.0
  cupertino_icons: ^0.1.2
  percent_indicator: ^1.0.10
  cloud_firestore: ^0.8.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_launcher_icons: "^0.7.0"

flutter_icons:
  image_path: "assets/images/GCF-logo.png" 
  android: true
  ios: true

flutter:

  uses-material-design: true
  assets:
    - assets/images/gcf_white.png
    - assets/images/login_back.png
    - assets/images/GCF-logo.png
